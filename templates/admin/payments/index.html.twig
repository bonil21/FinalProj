{% extends 'admin/_base.html.twig' %}
{% set active_menu = 'payments' %}
{% block title %}Payments - Admin{% endblock %}

{% block admin_content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="bi bi-credit-card"></i> Payments</h2>
    <a href="{{ path('app_subscription_index') }}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left"></i> Back to Subscriptions
    </a>
</div>

<form method="get" class="row g-2 mb-3">
    <div class="col-md-4">
        <input type="text" class="form-control" name="q" value="{{ search }}" placeholder="Search customer, method, description...">
    </div>
    <div class="col-md-3">
        <select class="form-select" name="status">
            <option value="">All statuses</option>
            {% for s in ['completed', 'succeeded', 'success', 'paid', 'pending', 'failed', 'cancelled'] %}
                <option value="{{ s }}" {% if statusFilter == s %}selected{% endif %}>{{ s|title }}</option>
            {% endfor %}
        </select>
    </div>
    <div class="col-md-2">
        <select class="form-select" name="limit">
            {% for n in [10, 20, 50, 100] %}
                <option value="{{ n }}" {% if limit == n %}selected{% endif %}>{{ n }} / page</option>
            {% endfor %}
        </select>
    </div>
    <div class="col-md-3 d-flex gap-2">
        <button class="btn btn-primary" type="submit">Apply</button>
        <a class="btn btn-outline-secondary" href="{{ path('app_payment_index') }}">Reset</a>
    </div>
</form>

<div class="row mb-3">
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <h4 class="text-success">{{ (statusCounts['completed'] ?? 0) + (statusCounts['succeeded'] ?? 0) + (statusCounts['success'] ?? 0) + (statusCounts['paid'] ?? 0) }}</h4>
                <small class="text-muted">Successful</small>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <h4 class="text-warning">{{ statusCounts['pending'] ?? 0 }}</h4>
                <small class="text-muted">Pending</small>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <h4 class="text-danger">{{ statusCounts['failed'] ?? 0 }}</h4>
                <small class="text-muted">Failed</small>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <h4 class="text-info">{{ totalPayments }}</h4>
                <small class="text-muted">Total</small>
            </div>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h5 class="mb-0">All Payments</h5>
    </div>
    <div class="card-body">
        {% if payments is empty %}
            <div class="text-center py-5">
                <i class="fas fa-credit-card fa-3x text-muted mb-3"></i>
                <h4 class="text-muted">No payments found</h4>
                <p class="text-muted">Payments will appear here when customers make purchases.</p>
            </div>
        {% else %}
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Customer</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th>Method</th>
                            <th>Created</th>
                            <th>Paid</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for payment in payments %}
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="avatar-sm bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-2">
                                            {{ payment.customer.name|first|upper }}
                                        </div>
                                        <div>
                                            <div class="fw-bold">{{ payment.customer.name }}</div>
                                            <small class="text-muted">{{ payment.customer.email }}</small>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <div class="fw-bold">${{ payment.amount }}</div>
                                    <small class="text-muted">{{ payment.currency|upper }}</small>
                                </td>
                                <td>
                                    {% set statusClass = {
                                        'completed': 'success',
                                        'succeeded': 'success',
                                        'success': 'success',
                                        'paid': 'success',
                                        'pending': 'warning',
                                        'failed': 'danger',
                                        'cancelled': 'secondary'
                                    } %}
                                    <span class="badge bg-{{ statusClass[payment.status] ?? 'secondary' }}">
                                        {{ payment.status|title }}
                                    </span>
                                </td>
                                <td>
                                    <span class="badge bg-info">{{ payment.paymentMethod|title }}</span>
                                </td>
                                <td>
                                    <div>{{ payment.createdAt|date('M d, Y') }}</div>
                                    <small class="text-muted">{{ payment.createdAt|date('H:i') }}</small>
                                </td>
                                <td>
                                    {% if payment.paidAt %}
                                        <div>{{ payment.paidAt|date('M d, Y') }}</div>
                                        <small class="text-muted">{{ payment.paidAt|date('H:i') }}</small>
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a href="{{ path('app_payment_show', {'id': payment.id}) }}" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}

        {% if totalPages > 1 %}
            <div class="d-flex justify-content-between align-items-center mt-3">
                <small class="text-muted">Page {{ page }} of {{ totalPages }}</small>
                <div class="btn-group">
                    {% set prevPage = page - 1 %}
                    {% set nextPage = page + 1 %}
                    <a class="btn btn-outline-secondary btn-sm {% if page <= 1 %}disabled{% endif %}" href="{{ path('app_payment_index', {page: prevPage, limit: limit, status: statusFilter, q: search}) }}">Prev</a>
                    <a class="btn btn-outline-secondary btn-sm {% if page >= totalPages %}disabled{% endif %}" href="{{ path('app_payment_index', {page: nextPage, limit: limit, status: statusFilter, q: search}) }}">Next</a>
                </div>
            </div>
        {% endif %}
    </div>
</div>

<style>
.avatar-sm {
    width: 32px;
    height: 32px;
    font-size: 14px;
}
</style>
{% endblock %}
